# Import helper package

This small package mirror exists only so the public example tests can import:

```python
from docs.context_boundary_lab.gate_api.interface import ExampleConservativeContextBoundary
```

Human-facing docs live in `docs/context-boundary-lab/`.
